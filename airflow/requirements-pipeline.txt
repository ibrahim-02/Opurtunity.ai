# Pipeline-only requirements for Airflow container.
# SQLAlchemy is intentionally omitted — Airflow manages its own version (1.4.x).

# Database
psycopg2-binary==2.9.10
pgvector>=0.3.0
pg8000==1.31.2
cloud-sql-python-connector[pg8000]==1.12.1

# GCS + Vertex AI
google-cloud-storage==2.18.2
google-genai>=1.74.0

# Scraping / parsing
beautifulsoup4==4.12.3
lxml==5.3.0

# HTTP (for scrapers)
httpx>=0.28.1

# Utilities
loguru==0.7.2
python-dotenv==1.0.1
pydantic==2.9.2
tenacity==9.0.0
